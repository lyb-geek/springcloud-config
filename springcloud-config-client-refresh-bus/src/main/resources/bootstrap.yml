spring:
  application:
    name: foobar
  cloud:
    config:
      uri: http://localhost:8080
      profile: dev
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  
 
  
    
     